# Copiar a secrets.toml para pruebas locales
# En Docker/Dokploy usar variables de entorno (CLIENT_*_ACCESS_TOKEN, etc.)

# [sources.facebook_ads]
# access_token = "tu_token_aqui"

# [destination]
# Para Postgres, las credenciales van en el pipeline.run() o en variables:
# POSTGRES_HOST, POSTGRES_PORT, POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DATABASE
